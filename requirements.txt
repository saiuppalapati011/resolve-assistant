# Core backend
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
websockets>=12.0
python-dotenv>=1.0.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
pyyaml>=6.0.1
aiofiles>=23.2.1

# LangChain / LangGraph
langchain>=0.2.0
langgraph>=0.1.0
langchain-anthropic>=0.1.0
langchain-community>=0.2.0
langchain-core>=0.2.0
langgraph-checkpoint-sqlite>=2.0.0
langchain-mcp-adapters>=0.1.0

# LLM clients
anthropic>=0.28.0
google-genai>=0.1.0
requests>=2.32.0

# RAG / Vector store
# chromadb ships with its own onnxruntime-based default embedder (all-MiniLM-L6-v2)
# so we do NOT need sentence-transformers (which pulls in the full PyTorch stack).
# We use chromadb's DefaultEmbeddingFunction which is onnx-based and lightweight.
chromadb>=0.5.0

# PDF processing
pdfplumber>=0.11.0
pypdf>=4.2.0

# Logging
structlog>=24.1.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-mock>=3.14.0
